// Los paquetes @fontsource(-variable) se importan solo por su efecto secundario
// (inyectan @font-face) y no proveen tipos. Declaramos los módulos para TS.
declare module '@fontsource-variable/*'
declare module '@fontsource/*'
