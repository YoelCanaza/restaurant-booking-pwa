import AppRoutes from './routes'

/**
 * App — Rincón Andino
 * Punto de entrada de la aplicación. Delegado completamente al
 * sistema de rutas. El BrowserRouter ya está configurado en main.tsx.
 */
export default function App() {
  return <AppRoutes />
}
